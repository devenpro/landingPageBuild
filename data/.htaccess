# Belt-and-braces: data/ lives at repo root, OUTSIDE site/public/ which is
# the doc root, so this file is purely defence-in-depth. If a misconfig
# ever exposes data/, this denies all access.
Require all denied
