-- 0002_seed.sql — placeholder. Phase 2 will populate content_blocks
-- with one row per editable key on the page. Kept as a no-op for now so
-- migrate.php has a stable file list to track.
SELECT 1;
